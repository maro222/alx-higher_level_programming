#!/usr/bin/python3
def best_score(a_dictionary):
    if not a_dictionary:
        return (None)
    mmax
    for k, v in a_dictionary.items():
        if not mmax or mmax < v:
            mmax = k
    return (mmax)
